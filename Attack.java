/*
package com.streetfightergame.game;

import com.badlogic.gdx.graphics.Texture;
import com.badlogic.gdx.graphics.g2d.SpriteBatch;

// WIP lol
public class Attack extends SpriteObject{
    public Attack(int defaultX, int defaultY, String pathToAsset, SpriteBatch batch, Animator animator){
        super(defaultX, defaultY, pathToAsset, batch);
    }
    private Texture currentTexture = new Texture("test.png");

    public void changeTexture(Texture newTexture){
        this.currentTexture = newTexture;
    }
    private float x;
    private float y;
    public void updatePosition(float x, float y){
        this.x = x;
        this.y = y;
    }
    @Override
    public void update(){
        System.out.println(getX());
        batch.draw(new Texture("test.png"), x, y); // Draw current frame at (50, 50)
    }
}
*/

/**
 * ERA PER UN TEST
 */